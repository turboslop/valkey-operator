pre-commit:
  parallel: true
  commands:
    lint:
      run: golangci-lint run ./...
    build:
      run: go build ./...
    test:
      run: go test $(go list ./... | grep -v /e2e) -short
    vet:
      run: go vet ./...
